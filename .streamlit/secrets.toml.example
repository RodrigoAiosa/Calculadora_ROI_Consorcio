# Copie este arquivo para .streamlit/secrets.toml (não versionado) e defina sua senha.
# Em produção (Streamlit Community Cloud): Settings → Secrets, cole a mesma chave/valor.

app_password = "troque-esta-senha"
